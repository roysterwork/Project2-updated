//the code for movies_tools.py starts by opening the cleaned data set from lab 4

//following this the we create a class called Movie, with instance variables title and rating, which give the title and rating for the each individual movie in the cleaned data, dataset.  

// the code includes a flask app so that the code can run on a browser/local server
// the flask app includes to routes, one which is a home route, and one which gives the movie title and rating for the corresponding movie.  
